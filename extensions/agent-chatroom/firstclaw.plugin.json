{
  "id": "agent-chatroom",
  "uiHints": {
    "nasRoot": {
      "label": "NAS Root Path",
      "placeholder": "\\\\FFUS_NAS\\Projects",
      "help": "Path to the NAS mount containing the chatroom directory"
    },
    "agentId": {
      "label": "Agent ID",
      "placeholder": "art",
      "help": "This agent's registered ID in the chatroom (e.g. art, audio, gamedev, uiux, firstclaw)"
    },
    "role": {
      "label": "Agent Role",
      "placeholder": "worker",
      "help": "Set to 'orchestrator' for FirstClaw only. All other agents must be 'worker'. Workers can only receive tasks via DM and cannot interact in #general."
    },
    "localDir": {
      "label": "Local Queue Directory",
      "placeholder": "./chatroom_local",
      "help": "Local directory for the daemon's pending message queue",
      "advanced": true
    },
    "pollIntervalMs": {
      "label": "Poll Interval (ms)",
      "placeholder": "3000",
      "help": "How often the daemon polls the NAS inbox",
      "advanced": true
    },
    "repoRoot": {
      "label": "FirstClaw Repo Root",
      "placeholder": "/Users/me/openclaw",
      "help": "Absolute path to the local FirstClaw git repository. Required for /system-update to work."
    },
    "chatroomServerUrl": {
      "label": "Chatroom Server URL",
      "placeholder": "http://192.168.1.100:8080",
      "help": "URL of the Chatroom web server. Used for RAG queries and other API calls."
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "nasRoot": {
        "type": "string"
      },
      "agentId": {
        "type": "string"
      },
      "role": {
        "type": "string",
        "enum": ["orchestrator", "worker"],
        "default": "worker"
      },
      "localDir": {
        "type": "string"
      },
      "pollIntervalMs": {
        "type": "number"
      },
      "repoRoot": {
        "type": "string"
      },
      "chatroomServerUrl": {
        "type": "string"
      }
    },
    "required": ["nasRoot", "agentId"]
  }
}
